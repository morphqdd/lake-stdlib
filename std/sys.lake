// Process-global termination.  `exit(code)` is ret-typed so `pin
// exit(1)` runs synchronously; rt_exit never returns.

@rt(rt_exit)

pub exit is {
  code i64 -> ret i64 {
    rt_exit(code)
    ret 0
  }
}
